### 1\. Клонування репозиторію


```bash
git clone [URL-вашого-репозиторію]
cd [назва-папки-проекту]
```

### 2\. Встановлення залежностей

Проект використовує Node.js та NPM. Переконайся, що вони в тебе встановлені. Потім, встанови всі необхідні пакети:

```bash
npm install
```

Ця команда автоматично завантажить `express`, `sqlite3` та `axios` у папку `node_modules`.

## 🏃 Запуск проекту

Щоб запустити сервер, виконай команду:

```bash
node server.js
```

Ти повинен побачити у своєму терміналі повідомлення:
`Сервер запущено! http://localhost:3000`

Тепер відкрий цю адресу (`http://localhost:3000`) у своєму браузері. Ти побачиш веб-інтерфейс для керування даними.

## 🗃️ Структура Бази Даних

Проект використовує **SQLite 3** — це легка файлова база даних. Усі дані зберігаються в одному файлі `data.db`, який автоматично створюється у папці проекту при першому запуску сервера.

### Логіка Збереження

Для запобігання дублікатам при повторному завантаженні тих самих даних, усі команди збереження використовують логіку `INSERT OR IGNORE`. Це означає, що якщо запис із таким `id` (який є `PRIMARY KEY`) вже існує, він **не буде** дубльований, а команда просто проігнорується.

### Таблиці

База даних має реляційну структуру (таблиці пов'язані між собою) і складається з 6 таблиць:

1.  **`users`**

      * `id` (INTEGER, PRIMARY KEY)
      * `name` (TEXT)
      * `username` (TEXT)
      * `email` (TEXT)

2.  **`posts`**

      * `id` (INTEGER, PRIMARY KEY)
      * `userId` (INTEGER) - *Зв'язок з `users.id`*
      * `title` (TEXT)
      * `body` (TEXT)

3.  **`albums`**

      * `id` (INTEGER, PRIMARY KEY)
      * `userId` (INTEGER) - *Зв'язок з `users.id`*
      * `title` (TEXT)

4.  **`todos`**

      * `id` (INTEGER, PRIMARY KEY)
      * `userId` (INTEGER) - *Зв'язок з `users.id`*
      * `title` (TEXT)
      * `completed` (INTEGER) - *1 (true) або 0 (false)*

5.  **`comments`**

      * `id` (INTEGER, PRIMARY KEY)
      * `postId` (INTEGER) - *Зв'язок з `posts.id`*
      * `name` (TEXT)
      * `email` (TEXT)
      * `body` (TEXT)

6.  **`photos`**

      * `id` (INTEGER, PRIMARY KEY)
      * `albumId` (INTEGER) - *Зв'язок з `albums.id`*
      * `title` (TEXT)
      * `url` (TEXT)
      * `thumbnailUrl` (TEXT)
